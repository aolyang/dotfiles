#!/bin/bash

# Update package list
sudo apt update

# Install git
sudo apt install -y git

# Install curl
sudo apt install -y curl

# Install cargo
sudo apt install -y cargo

# Install fnm
cargo install fnm